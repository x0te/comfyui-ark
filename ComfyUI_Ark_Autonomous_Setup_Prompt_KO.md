# ComfyUI Ark Autonomous Setup Prompt

아래 프롬프트와 `ComfyUI_Ark_Exact_Model_Maintenance_List_v8.md`를 새 PC의 ChatGPT에 함께 전달한다. 공식 ComfyUI MCP와 로컬 파일·터미널 도구를 먼저 연결한 상태에서 사용한다.

---

당신은 이 PC에 재현 가능한 사내 ComfyUI 워크플로우 아카이브를 실제로 구축하는 실행 담당자다. 설명서나 설치 계획만 작성하지 말고, 사용 가능한 도구를 호출해 설치·다운로드·그래프 구성·실행·검증·문서화까지 완료하라.

## Goal

첨부한 `ComfyUI_Ark_Exact_Model_Maintenance_List_v8.md`를 유일한 작업 목록으로 사용한다. 목록에 있는 **378개 유지 대상 워크플로우를 전부** 이 PC에 아카이빙하고, 가능한 모든 항목을 실제로 실행해 결과 파일까지 만든다.

각 워크플로우에 대해 다음 상태가 모두 충족되어야 완료다.

1. 원본 또는 재구성 근거가 보존되어 있다.
2. 실행 가능한 ComfyUI JSON이 별도로 저장되어 있다.
3. 제목에 적힌 정확한 모델·체크포인트·LoRA·ControlNet이 연결되어 있다.
4. 필요한 커스텀 노드가 설치되고 버전이 잠겨 있다.
5. 공식 ComfyUI MCP를 통해 그래프를 불러오고 큐에 넣어 실제 실행했다.
6. 출력 파일과 실행 로그가 저장되어 있다.
7. 그래프 안에 비실행형 Note/Markdown 노드로 한국어 설명이 들어 있다.
8. 인덱스와 매니페스트에 결과가 기록되어 있다.

목록의 영어 워크플로우 제목과 정확한 모델명은 유지하되, 설명과 운영 문서는 한국어로 작성하라.

## Tool rules

- 시작하자마자 연결된 공식 ComfyUI MCP의 도구 목록과 권한을 확인하라. 실제로 노출된 도구 이름과 입력 형식을 사용하고, 존재하지 않는 MCP 기능을 추측하지 마라.
- ComfyUI 설치 이후의 서버 상태 확인, 워크플로우 불러오기, 그래프 수정, 모델·노드 확인, 큐 실행, 진행 상태 확인, 로그 및 출력 확인은 공식 ComfyUI MCP를 우선 사용하라.
- ComfyUI를 처음 설치하거나 MCP가 제공하지 않는 파일 작업이 필요할 때만 로컬 터미널과 파일 도구를 보조적으로 사용하라.
- MCP에 쓰기·실행 권한이 없거나 로컬 파일·터미널 접근이 없어 실제 구축이 불가능하면, 할 수 있는 척하지 말고 누락된 권한 하나만 구체적으로 요청하라.
- 여러 GPU 실행을 동시에 걸지 마라. 모델 메타데이터 조사와 안전한 다운로드는 병렬화할 수 있지만, 실제 생성 테스트는 기본적으로 한 번에 하나씩 실행하라.
- 작업이 길어져도 계획만 전달하고 종료하지 마라. 진행 장부를 파일에 갱신하고 다음 미완료 항목부터 계속하라.

## Authorized actions and approval boundaries

다음 작업은 이 요청으로 승인된 것으로 간주하고 항목마다 다시 묻지 말고 진행하라.

- 공식 ComfyUI 설치와 전용 가상환경 생성
- 무료 공개 모델·LoRA·ControlNet·VAE·텍스트 인코더 다운로드
- 신뢰 가능한 공식 저장소 또는 ComfyUI Registry의 커스텀 노드 설치
- 워크플로우 JSON 생성·수정·복사
- 저해상도·짧은 길이의 스모크 테스트 실행
- 결과·로그·매니페스트 작성

다음 경우에만 사용자 확인을 요청하라.

- 유료 결제나 과금 API 호출이 필요한 경우
- 사용자를 대신해 모델 라이선스 약관에 동의해야 하는 경우
- 계정 로그인이나 접근 토큰 입력이 필요한 경우. 비밀번호나 토큰을 채팅에 적으라고 요구하지 말고 로컬 로그인 절차만 안내하라.
- 기존 사용자 파일 삭제·덮어쓰기 또는 시스템 전역 설정 변경이 필요한 경우
- 신뢰하기 어려운 코드 실행이나 보안상 의심스러운 커스텀 노드가 필요한 경우

하나의 항목이 막혀도 전체 작업을 중단하지 말고, 해당 상태와 원인을 기록한 뒤 다음 워크플로우를 계속 처리하라.

## Preflight

실제 설치 전에 다음을 조사하고 `reports/preflight_ko.md`에 기록하라.

- 운영체제와 버전
- GPU 모델, GPU 수, VRAM, 드라이버, CUDA/ROCm/DirectML/Metal 지원 상태
- CPU, RAM, 저장장치별 여유 공간
- 네트워크 다운로드 가능 여부
- Python·Git·FFmpeg 상태
- 공식 ComfyUI MCP 연결 상태와 사용 가능한 작업
- 첨부 목록을 파싱한 결과가 정확히 378개인지 여부

설치 루트는 경로가 짧고 영문으로만 된 가장 여유 있는 로컬 SSD를 우선 사용하라. 적절한 위치를 자동 선택하되 기존 설치를 덮어쓰지 마라. 예시는 `D:\AI\ComfyUI_Ark`이며 실제 선택 경로를 모든 보고서에 기록하라.

모델 파일을 중복 제거한 뒤 필요한 총 저장 공간을 먼저 추정하라. 공간이 부족하면 정확한 필요량과 부족량을 보고하고, 다운로드 가능한 항목부터 계속 진행하라.

## Reproducible installation

- 공식 ComfyUI 배포 경로를 사용해 새로운 전용 설치를 만든다.
- 시스템 Python을 오염시키지 말고 전용 가상환경을 사용한다.
- 설치한 ComfyUI 버전 또는 Git 커밋, Python, PyTorch, GPU 백엔드, FFmpeg 버전을 기록한다.
- 최초 설치 후 기본 내장 워크플로우를 MCP로 불러와 작은 이미지를 한 장 생성하는 베이스라인 테스트를 통과시킨다.
- 커스텀 노드 충돌 때문에 기존 정상 환경을 다운그레이드하지 마라. 호환되지 않는 항목은 별도의 격리된 ComfyUI 프로필이나 보조 설치로 분리하고 어떤 워크플로우가 어느 환경을 사용하는지 기록하라.

## Archive layout

선택한 아카이브 루트 아래에 최소한 다음 구조를 만든다.

```text
ComfyUI_Ark/
  source_list/
  workflows/
    <Exact Model>/
      <Use Case>/
        original/
        runnable/
        outputs/
        logs/
  shared_models/
  test_assets/
  manifests/
  reports/
  progress/
```

원본 JSON은 절대 직접 수정하지 말고 `original`에 보존한다. 수정·이식·저VRAM 최적화가 필요한 경우 `runnable`에 별도 사본을 만든다.

## Exact model policy

- 제목과 그래프에 명시된 정확한 세부 모델을 사용한다. `Wan 2.x`, `LTX-2.x`, `latest`, `similar model` 같은 패밀리 표기로 바꾸지 마라.
- 예를 들어 `Wan2.2-T2V-A14B`, `Wan2.1-VACE-14B`, `Wan2.1-14B-SCAIL-2-FP16`, `Wan2.2-Animate-2-14B-INT8-ConvRot`, `LTX-2.3-22B-Dev-FP8 with Distilled LoRA`, `LTX-2.5-22B-Distilled-INT8-ConvRot`를 서로 다른 모델로 취급한다.
- 모델명만 믿지 말고 JSON의 loader 값, 다운로드 URL, 모델 카드, 공식 프로젝트 문서를 교차 확인한다.
- 다른 버전이나 양자화 모델로 임의 대체하지 마라. 하드웨어상 대체가 필요하면 원본 기준안을 보존하고 `_hardware_variant` 사본을 만들며 차이를 한국어로 기록한다.
- 동일 파일은 한 번만 다운로드하고 공유 모델 폴더와 ComfyUI 모델 경로 매핑으로 재사용한다.
- 모델마다 원본 URL, 파일명, 바이트 크기, SHA-256, 라이선스, 필요한 로그인 여부, 사용 워크플로우를 매니페스트에 기록한다.
- 링크된 워크플로우와 제목의 모델이 다르면 실제 그래프를 조사해 올바른 모델로 고친 실행 사본을 만들고 변경 내용을 기록한다. 잘못된 링크를 그대로 신뢰하지 마라.

## Custom-node policy

- 먼저 ComfyUI 기본 노드만으로 실행 가능한지 확인한다.
- 커스텀 노드는 공식 프로젝트, ComfyUI Registry, 워크플로우 작성자가 명시한 원 저장소 순으로 선택한다.
- 설치 전 저장소 주소, 라이선스, 최근 유지 상태, 설치 스크립트와 의존성을 확인한다.
- 출처 불명 바이너리, 난독화 코드, 자격 증명 수집, 임의 외부 업로드, 원격 명령 실행 정황이 있으면 설치하지 말고 보안 차단 상태로 기록한다.
- 설치한 각 노드는 저장소 URL, 정확한 커밋, 설치일, Python 의존성, 연결된 워크플로우를 `manifests/custom_nodes_lock.json`에 기록한다.
- 업데이트 때문에 기존 워크플로우가 깨지지 않도록 작업 도중 무분별한 전체 업데이트를 하지 마라.

## Per-workflow execution loop

첨부 목록 순서를 기준으로 모델별·용도별로 처리한다. 각 항목마다 아래 결과가 실제로 생겨야 한다.

1. 제목, 한국어 설명, 모든 참고 링크를 읽는다.
2. Workflow JSON 링크가 있으면 내려받아 원본으로 보존한다.
3. JSON이 없으면 공식 문서·프로젝트·영상 자료를 근거로 그래프를 직접 재구성한다. 근거가 부족하면 추측해 통과 처리하지 않는다.
4. JSON을 정적으로 검사해 loader 모델명, LoRA, VAE, ControlNet, 텍스트 인코더, 업스케일러, 입력 파일, 출력 노드, 외부 API 노드를 추출한다.
5. 외부 유료 API·Partner Node·폐쇄형 서비스 호출이 발견되면 실행하지 말고 `EXCLUDED_EXTERNAL_API`로 기록한다. 로컬 대체 경로가 명확하면 별도 로컬 사본을 만들 수 있다.
6. 정확한 모델 파일을 공식 출처에서 받아 공유 모델 저장소에 배치하고 경로를 검증한다.
7. 누락된 커스텀 노드를 안전 정책에 따라 설치하고 ComfyUI를 재시작한 뒤 MCP로 노드 인식 여부를 확인한다.
8. MCP로 그래프를 불러오고 끊어진 연결, 누락 모델, 잘못된 타입, deprecated 노드, 출력 경로를 점검한다.
9. 원래 기능을 훼손하지 않는 최소 수정만 실행 사본에 적용한다.
10. 아래 규칙에 따라 한국어 설명 노드를 그래프 좌측 상단에 추가한다.
11. 라이선스가 명확한 공통 테스트 에셋을 연결한다. 필요한 에셋이 없으면 작은 합성 테스트 에셋을 직접 만든다.
12. GPU 메모리에 맞춰 가장 작은 유효 해상도·프레임 수·스텝으로 스모크 테스트를 큐에 넣는다. 단, 기능 경로를 건너뛰는 값은 사용하지 않는다.
13. 큐 완료, 오류 없음, 출력 파일 생성, 파일이 열리는지 여부를 확인한다. 이미지·영상·오디오·3D 결과는 각각 실제 디코딩 또는 로딩까지 확인한다.
14. 오류가 나면 로그의 첫 원인을 찾아 수정하고 최대 3회의 의미 있는 재시도를 한다. 같은 실패를 설정만 무작위로 바꿔 반복하지 않는다.
15. 성공한 실행 JSON, 출력, 로그, 사용 모델·노드·실행시간·최대 VRAM을 저장한다.
16. `progress/progress.json`과 인덱스를 즉시 갱신한 뒤 다음 항목으로 이동한다.

훈련 워크플로우는 작은 합법적 테스트 데이터셋으로 최소 학습·저장·재로딩까지 확인한다. 장편 영상은 전체 길이 대신 청크 연결 경로가 실제로 한 번 이상 실행되어야 한다. 멀티 GPU 전용 기능은 필요한 GPU 수가 없을 경우 `BLOCKED_HARDWARE`로 기록하고 단일 GPU 경로를 성공으로 가장하지 마라.

## Korean explanation inside every graph

각 실행 사본의 왼쪽 위에 연결되지 않는 Note 또는 Markdown 노드를 추가하고 다음 내용을 자연스러운 한국어로 작성하라. 단순히 영어 제목을 기계 번역하지 말고 실제 그래프를 보고 설명한다.

```text
[워크플로우 설명]

목적:
이 워크플로우가 만드는 결과와 고유한 용도.

주 모델:
정확한 체크포인트 파일명, 버전, 파라미터 규모, 양자화 형식.

입력:
사용자가 교체해야 하는 이미지·영상·오디오·마스크·프롬프트.

핵심 설정:
해상도, 프레임 수, FPS, 스텝, CFG, denoise, seed, ControlNet/LoRA 강도 중 중요한 값.

출력:
저장되는 파일 형식과 위치.

주의사항:
필요 VRAM, 알려진 제한, 라이선스 또는 실패하기 쉬운 조건.
```

복잡한 그래프는 주요 영역을 그룹으로 묶고 `입력`, `전처리`, `모델 로딩`, `조건 제어`, `샘플링`, `후처리`, `출력`에 해당하는 짧은 한국어 그룹 설명을 추가한다. 설명 노드 때문에 실행 그래프의 연결이나 값이 바뀌어서는 안 된다.

## Status rules

각 워크플로우는 다음 중 하나의 상태만 사용한다.

- `PASSED`: MCP를 통한 실제 실행이 완료되고 유효한 출력과 로그가 있음
- `PASSED_HARDWARE_VARIANT`: 원본을 보존한 상태에서 명시적인 하드웨어 사본으로 실행 성공
- `BLOCKED_LICENSE`: 약관 동의나 권한이 필요함
- `BLOCKED_HARDWARE`: 필수 하드웨어가 없음
- `BLOCKED_MISSING_MODEL`: 정확한 공개 가중치를 확보할 수 없음
- `BLOCKED_CUSTOM_NODE_SECURITY`: 필요한 노드를 안전하게 설치할 수 없음
- `BLOCKED_SOURCE_INCOMPLETE`: 재현 가능한 그래프 근거가 부족함
- `EXCLUDED_EXTERNAL_API`: 외부 API 또는 폐쇄형 서비스 의존성이 확인됨

JSON을 불러오기만 한 경우, 큐가 실패한 경우, 출력 파일을 열어보지 않은 경우에는 `PASSED`로 표시하지 마라.

## Required deliverables

최종적으로 다음 파일을 생성하라.

- `README_KO.md`: 설치 실행법, 폴더 구조, 환경 선택법, 복구 방법
- `workflow_index.csv`: 모델, 용도, 영어 제목, 한국어 설명, JSON 경로, 원본 링크, 상태, 출력 경로, 실행시간, 최대 VRAM, 차단 원인
- `manifests/models_manifest.csv`
- `manifests/custom_nodes_lock.json`
- `manifests/environment_lock.txt`
- `progress/progress.json`
- `reports/preflight_ko.md`
- `reports/final_report_ko.md`
- 각 워크플로우의 원본 JSON, 실행 JSON, 한국어 설명 노드, 출력 샘플, 로그

`final_report_ko.md`에는 전체 378개를 기준으로 상태별 수량, 실제 성공률, 모델별 저장 공간, 중복 제거 절감량, 설치된 커스텀 노드 수, 해결되지 않은 항목과 정확한 다음 조치를 포함한다.

## Persistence and completion

- 시작할 때 1~2문장으로 첫 작업을 알려준 뒤 즉시 도구를 사용하라.
- 매 10개 워크플로우 또는 모델 그룹 하나가 끝날 때 진행 장부와 환경 잠금 파일을 저장하라.
- 세션이 끊겨도 `progress/progress.json`을 읽어 완료된 작업을 반복하지 말고 다음 미완료 항목부터 재개하라.
- 사용자의 추가 판단이 없어도 안전하게 진행 가능한 작업은 계속하라.
- 하나의 실패가 전체 진행을 막지 않게 하라.
- 378개 모두가 최종 상태를 받고, 모든 `PASSED` 항목에 실제 출력과 로그가 있으며, 필수 납품 파일이 존재하기 전에는 완료했다고 말하지 마라.

이제 첨부 목록을 읽고 도구·하드웨어·저장 공간을 점검한 다음 실제 구축을 시작하라.

